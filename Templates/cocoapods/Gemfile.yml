name: "Gemfile"
mode: "root"
content: |
  source 'https://rubygems.org' do
    gem 'lefthook', '{{custom.lefthookVersion}}'
    gem 'xcpretty', '{{custom.xcprettyVersion}}'
    gem 'cocoapods', '{{custom.cocoapodsVersion}}'
    gem 'cocoapods-binary', '{{custom.cocoapodsBinaryVersion}}'
  end
