name: Test

on: [push, pull_request]

jobs:
  build:
    runs-on: macos-10.15
    steps:
      
    # Checkout
    - uses: actions/checkout@v2

    # Ruby setup
    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: '2.7'

    # Caches
    - uses: actions/cache@v1
      with:
        path: Examples/swift/Carthage
        key: ${{ runner.os }}-carthage-${{ hashFiles('**/Cartfile.resolved') }}-${{ hashFiles('Examples/swift/Carthage/**') }}
        restore-keys: |
          ${{ runner.os }}-carthage-
    - uses: actions/cache@v1
      with:
        path: .build
        key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}-${{ hashFiles('.build/**') }}
        restore-keys: |
          ${{ runner.os }}-spm-
    - uses: actions/cache@v1
      with:
        path: /usr/local/bin/mint
        key: ${{ runner.os }}-mint-${{ hashFiles('/usr/local/bin/mint') }}
        restore-keys: |
          ${{ runner.os }}-mint-

    # Setup
    - name: Setup
      run: |
        sh scripts/setup.sh
    
    # Tests
    - name: Tests
      run: swift test